[project]
name = "new"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nbconvert>=7.16.6",
    "pyyaml>=6.0.3",
    "scholarly>=1.7.11",
]
